fn invalid_nested(Memory assets_memory) returns Int {
    place in assets_memory {
        place in assets_memory {
            new Text msg = "bad"
            output(msg)
        }
    }

    return 0
}
